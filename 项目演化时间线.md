# CC Project 项目演化时间线

> 本报告由 git 提交历史、运行日志、现存对话记录交叉还原，用于补足已丢失的五月搭建对话。
> 生成时间：2026-08-27

---

## 一、项目概述

| 项 | 内容 |
|---|---|
| 项目定位 | 业务数据清洗与未回销看板 |
| 技术栈 | Python 3.10+ / Streamlit / Pandas / pytest |
| Git 远程 | `git@github.com:Matata0826/excelexport.git` |
| 业务领域 | 车险/商险「未回销清单」清洗、机构级联筛选、看板展示与导出 |

**核心链路**：上传 Excel → 自动清洗（列规范化 / 无效数据清理 / 主号映射 / 应回天数计算 / 标记未回销）→ 看板展示 → 筛选导出。

---

## 二、完整时间线

### 2026-05-25 之前 —— 实际编码起点（⚠️ 对话已丢失）

- `logs/` 目录最早的运行日志始于 **05-25 23:35**，此时清洗管道已具备 5 个步骤并可完整运行。
- 说明实际编码发生在此之前，但**对应的 Claude Code 对话记录已无法找回**（见第五节）。

### 2026-05-25 深夜 ~ 05-26 —— 开发与首次提交

- 运行日志显示 05-25 至 05-26 密集调试（2 + 11 次运行）。
- **05-26 00:51** 首次提交 `31f7d79`「业务数据清洗与未回销看板 — 初始版本」：
  - 26 个文件，+1346 行
  - 建立了完整骨架：`app.py`（Streamlit 主入口）、`config.yaml`（列映射/阈值/分组规则）、`src/` 模块（config_loader、audit、pipeline、dashboard、exporter）、`src/steps/` 可插拔清洗步骤、`tests/` 测试套件、`templates/mapping_template.xlsx`
  - 说明初始版本是「配置驱动 + 可插拔步骤」的模块化架构。

### 2026-05-27 —— 兼容性修复

- **05-27 01:23** 提交 `bba3d8d`「修复 Excel 导入 xlrd 兼容性问题，新增机构级联筛选与异常保护」：
  - 改动 7 个文件（+148 / -39）
  - 核心：`app.py` 大幅增强（+120 行），修复 `.xls` 导入的 xlrd 兼容问题，新增机构级联筛选与异常保护。

### 2026-05-29 —— 开发告一段落

- 最后一批运行日志（13:35 / 13:54 / 15:00），此后进入停用期。

### 2026-08-14 —— 看板侧边栏筛选调整（现存最早对话）

- 会话 `d6691eb2`「调整未回销看板侧边栏筛选」，28 条消息。
- 内容：围绕 `未回销看板.html` 侧边栏筛选「显示不全」问题（用户消息「这里显示不全」）。

### 2026-08-18 ~ 08-21 —— Excel 数据清洗系统架构设计（另一项目）

- 会话 `d0689d69`「Excel 数据清洗系统架构设计」，1069 条消息（体量最大）。
- ⚠️ 注意：这是**另一个项目**——通用「Excel Data Cleaning Skill」（Reader / Profiler / 清洗引擎 / Mapping / Export / React 前端），最终上传至 GitHub。
- 与 CC Project 同属数据清洗主题、且当时在 `~` 目录下运行，但与 CC Project 的「未回销看板」是两套独立系统。

### 2026-08-27 —— 当前

- 存在一批**未提交改动**（见第三节），为 8 月工作成果。

---

## 三、当前未提交改动清单（8 月工作）

`git diff` 统计（10 个文件，+291 / -24）：

| 文件 | 变化 |
|---|---|
| `tests/test_calc_due_days.py` | +124 |
| `src/steps/calc_due_days.py` | +90 |
| `app.py` | +72 |
| `requirements.txt` | 改动 |
| `src/pipeline.py` | +6 |
| `config.yaml` | 改动 |
| `src/dashboard.py` / `src/exporter.py` | 各 -2/+2 |
| `tests/conftest.py` / `tests/test_pipeline.py` | 小改 |

新增未跟踪文件（面向同事分发与业务数据）：

- `未回销看板.html`、`同事使用说明.txt`
- `setup_run.bat` / `setup_run.sh`（Windows / Mac 一键启动脚本）
- 业务数据：`26年度（不含宁波）企业车回销汇总 罚则(1).xls`、`副本商险26年度未回销清单26.5.24.xlsx`、`副本车险26年度截止5.24未回销清单（不含宁波）.xls`、`商险26年度未回销清单（含宁波）(1).xlsx`、`匹配.xlsx`

---

## 四、数据观察

1. **清洗步骤从 5 步缩减为 4 步**：早期日志（05-25）显示管道含 `mark_unwritten` 共 5 步；当前 `src/steps/` 目录已无 `mark_unwritten.py`（仅 `__pycache__` 残留 `.pyc`），推断该步骤在后续演进中被移除或合并。
2. **项目从「开发工具」演进为「可分发工具」**：新增 `setup_run.bat/.sh` 与「同事使用说明.txt」，说明 8 月工作重心转向让零基础同事可直接上手。
3. **代码活跃度与对话记录错位**：运行日志证明 5 月 25~29 日是真实开发高峰，但该阶段对话零留存，仅能靠日志 + git 还原。

---

## 五、关于「丢失的五月对话」的结论

| 恢复途径 | 结果 |
|---|---|
| Time Machine | 未配置 |
| 废纸篓 | 无相关文件 |
| `.bak` / `.old` 备份 | 无 |
| iCloud 云同步 | 无 `.claude` |
| 全局 `history.jsonl` | 五月无 CC 项目记录 |
| 终端命令历史 | 无「未回销/车险/商险/承保」痕迹 |

**结论**：五月初始版本的搭建对话在 Claude Code 历史中已不存在，且大概率**非本机 Claude Code 产生**（终端历史亦无痕迹）。本报告已用 git 提交 + 运行日志 + 现存对话，尽可能还原了这段演化过程。
